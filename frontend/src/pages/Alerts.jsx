export default function Alerts({alerts}){return <div className="page"><div className="page-head"><div><div className="eyebrow">OPERATIONS</div><h1>Alert queue</h1></div></div><div className="alert-list">{alerts.length===0?<div className="empty-card">No alerts yet. Select an event and create one from the event panel.</div>:alerts.map(a=><div className="alert-row" key={a.id}><span className={`risk-badge ${a.severity.toLowerCase()}`}>{a.severity}</span><div><b>{a.hotspot_id}</b><p>{a.message}</p><small>{a.facility_name||'Unknown facility'} · {new Date(a.created_at).toLocaleString()}</small></div><em>{a.status}</em></div>)}</div></div>}
